# shellcheck shell=bash

format:
   #!/usr/bin/env bash
   fourmolu -i src app test

hlint:
  #!/usr/bin/env bash
  hlint src

test:
  #!/usr/bin/env bash
  cabal test

CI:
  #!/usr/bin/env bash
  set -euo pipefail
  fourmolu -m check src app test
  hlint -c src app test
  just test